.git
bin/
*.exe
*.test
coverage.out
.env
.env.*
!.env.example
.idea/
.vscode/
.DS_Store

# The advisory model artifact lives in ./ollama for its own build context;
# the backend image never needs it, and at 261 MB it would otherwise bloat
# every `docker build` of the backend for no reason.
ollama/model
*.gguf
